#include "raylib.h"

#define WINDOW_WIDTH (short)800
#define WINDOW_HEIGHT (short)800
#define LEAFS (int)8 // Make a power of 2 because binary

typedef struct Leaf
{
    Rectangle rec;
} Leaf;

int main()
{
    Leaf rooms[LEAFS];

    

    return 0;
}